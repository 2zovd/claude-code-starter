# Copy to .env and fill in values. Never commit .env.
# Nuxt env vars: prefix with NUXT_ for runtimeConfig overrides.
# Public vars (exposed to client): prefix with NUXT_PUBLIC_

# Server-only secrets
NUXT_API_SECRET=

# Public runtime config
NUXT_PUBLIC_API_BASE=http://localhost:3000
