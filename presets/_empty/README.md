# _empty preset

This preset has no files by design.

Use it with a stack recipe:

1. Pick the recipe that matches your stack from [`stack-recipes/`](../../stack-recipes/)
2. Tell Claude: `Read INIT.md and bootstrap this project` — it will apply the recipe for you
3. Or follow the recipe manually if you prefer

Available recipes: `node-bot.md`, `python-uv.md`, `astro.md`, `component-library.md`
